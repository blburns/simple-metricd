# simple-metricd high-security template (TLS/ACLs arrive in later milestones)

listen_address = 127.0.0.1
listen_port = 19100
log_level = warning
foreground = false

metric = simple_metricd_up gauge help=daemon process up
